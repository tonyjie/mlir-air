# Debug log — smollm2_1_7b

(Append entries when a debug recipe fires — e.g., `debug-bo-corruption`,
`debug-multi-launch-merge`. Format per entry: date, phase, symptom,
recipe used, root cause, fix.)
